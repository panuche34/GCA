using Core.Enumerators;
using Core.ViewModel;
using Core.VO;

namespace Application.Interface
{
    public interface IListService
    {
        //Task<IEnumerable<AutoCompleteVO>> AutocompleteAsync(TypeTagHelperEntity type, 
        //    string term, UserVM userLogged, int? idToFilter = null);
        //Task<IEnumerable<AutoCompleteVO>> GetSelectAsync(TypeTagHelperEntity type);
        //Task<DataTableResult<SearchDatatableVM>> SearchForDataTableAsync(
        //    DataTableAdvancedVM<SearchDatatableFilterVM> filter, UserVM userLogged);
    }
    
}
